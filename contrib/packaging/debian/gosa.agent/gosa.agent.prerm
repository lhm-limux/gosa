#!/bin/sh

set -e

#DEBHELPER#

if [ "$1" = "purge" ] ; then
    deluser --quiet --system gosa-agent > /dev/null || true
    delgroup --quiet --system gosa-agent > /dev/null || true

    f=/var/run/gosa-agent
    if [ -d "$f" ]; then
        rmdir "$f" || { chown root:root "$f" && chmod 00700 "$f"; }
    fi
fi
